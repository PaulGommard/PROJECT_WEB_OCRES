import React from "react";
import '../index.css';

const Graphique = ({item}) => (
    <div class="graph">
        <div class="title">
            <h2>Graphique</h2>
        </div>      
    </div>
    );

export default Graphique;