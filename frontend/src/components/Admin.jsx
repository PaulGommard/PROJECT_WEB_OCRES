import React from "react";
import '../index.css';

const Admin = ({item}) => (
    <div class="admin">
        <div class="title">
            <h2>Admin</h2>
        </div>

        <div className="description">
            
        </div>
        
    </div>
    );

export default Admin;